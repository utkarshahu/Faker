<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Chats</title>
    <style>
        body {
            font-family: "Arial", sans-serif;
            background-color: #2f3136; /* Discord Dark Theme */
            color: white;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #5865F2; /* Discord Blue */
            font-size: 2rem;
            margin-bottom: 2rem;
        }

        .chat-form {
            background-color: #40444b; /* Dark Grey for form */
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            max-width: 600px;
            margin: 0 auto;
        }

        label {
            font-size: 1rem;
            margin-bottom: 0.5rem;
            color: #b9bbbe; /* Light Grey for text */
        }

        .chat-form input, .chat-form textarea {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border-radius: 8px;
            border: 1px solid #b9bbbe;
            background-color: #2f3136; /* Same as body */
            color: white;
            font-size: 1rem;
        }

        .chat-form input:focus, .chat-form textarea:focus {
            outline: none;
            border-color: #5865F2; /* Blue border on focus */
        }

        .chat-form textarea {
            height: 150px;
            resize: none;
        }

        .chat-form button {
            background-color: #5865F2; /* Discord Blue */
            color: white;
            padding: 12px 20px;
            font-size: 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            transition: background-color 0.3s;
        }

        .chat-form button:hover {
            background-color: #4752C4; /* Darker Discord Blue */
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }

            .chat-form {
                padding: 15px;
                width: 90%;
            }
        }
    </style>
</head>
<body>

    <h1>Write New Chats</h1>

    <div class="chat-form">
        <form action="/chats" method="post">
            <label for="from">Your Name:</label>
            <input type="text" id="from" name="from" placeholder="Who are you?" required>
            
            <label for="msg">Your Message:</label>
            <textarea name="msg" id="msg" placeholder="Write your message..." required></textarea>
            
            <label for="to">Recipient:</label>
            <input type="text" id="to" name="to" placeholder="Who are you messaging?" required>
            
            <button type="submit">Send Message</button>
        </form>
    </div>

</body>
</html>
