<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body, html {
            font-family: 'Arial', sans-serif;
            background-color: #36393F; /* Discord Dark Background */
            color: white;
            height: 100%;
            width: 100%;
        }

        /* Main Container */
        .main {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 3rem;
            background: linear-gradient(145deg, #2f3136, #23272a); /* Gradient background for depth */
            border-radius: 20px;
            min-height: 100vh;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.6); /* Soft shadow for depth */
            transition: transform 0.3s ease-in-out;
        }

        .main:hover {
            transform: scale(1.02); /* Slight zoom effect on hover */
        }

        /* Header Section */
        .header {
            background-color: #2c2f33; /* Slightly darker background for header */
            width: 100%;
            padding: 1.5rem;
            text-align: center;
            border-radius: 10px;
            margin-bottom: 2rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .discord-logo {
            width: 50px;
            height: 50px;
            vertical-align: middle;
        }

        h1 {
            margin-top: 1rem;
            font-size: 1.8rem;
            font-weight: 600;
        }

        /* Form Container */
        .form-container {
            background-color: #2f3136; /* Dark background for the form */
            width: 100%;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4); /* Subtle shadow for form */
            border: 1px solid #3a3d42; /* Light border for form */
        }

        /* Textarea Styling */
        .chat-textarea {
            width: 100%;
            height: 200px;
            background-color: #40444B; /* Dark background for textarea */
            color: white;
            border: none;
            border-radius: 8px;
            padding: 1rem;
            font-size: 1rem;
            resize: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 1.5rem;
            transition: background-color 0.3s ease-in-out;
        }

        .chat-textarea:focus {
            background-color: #484B52; /* Lighter background color on focus */
            outline: none;
        }

        /* Edit Button */
        .edit-btn {
            background-color: #5865F2; /* Discord Blue */
            color: white;
            border: none;
            padding: 1rem 2rem;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
            width: 100%;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        .edit-btn:hover {
            background-color: #4752C4; /* Darker Discord Blue on hover */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3); /* Shadow effect on hover */
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .main {
                padding: 2rem;
            }

            .header {
                padding: 1rem;
                text-align: center;
            }

            .discord-logo {
                width: 40px;
                height: 40px;
            }

            .chat-textarea {
                height: 150px;
            }

            .edit-btn {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="main">
        <div class="header">
            <img src="https://www.edigitalagency.com.au/wp-content/uploads/Discord-logo-icon-clyde-blurple-png-600x456.png" alt="Discord Logo" class="discord-logo">
            <h1>Edit Your Chat: <%= chat._id %></h1>
            <p>This chat was sent from <%= chat.from %> to <%= chat.to %></p>
        </div>

        <div class="form-container">
            <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
                <textarea name="msg" class="chat-textarea"><%= chat.msg %></textarea>
                <button type="submit" class="edit-btn">Edit</button>
            </form>
            
        </div>
    </div>
</body>
</html>
